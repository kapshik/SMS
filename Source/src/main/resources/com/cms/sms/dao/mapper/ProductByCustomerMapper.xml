<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cms.sms.dao.mapper.ProductByCustomerMapper">

	<sql id="whereStatements">
		<where>
			<if test="customerNo != null and customerNo != ''">
				AND customerNo = #{customerNo}
			</if>
			<if test="productCode != null and productCode != ''">
				AND productCode = #{productCode}
			</if>
			<if test="discountPrice != null and discountPrice != ''">
				AND discountPrice = #{discountPrice}
			</if>
		</where>
	</sql>

    <select id="findOne" parameterType="com.cms.sms.dao.domain.ProductByCustomer" resultType="com.cms.sms.dao.domain.ProductByCustomer">
		SELECT
			*
		FROM
			tbl_productByCustomer
		WHERE
			customerNo =  #{customerNo}
			AND productCode =  #{productCode}
    </select>

    <select id="findList" parameterType="com.cms.sms.dao.domain.ProductByCustomer" resultType="com.cms.sms.dao.domain.ProductByCustomer">
		SELECT
			* 
		FROM
			tbl_productByCustomer
		<include refid="whereStatements" />
		ORDER BY
			customerNo, productCode
    </select>

    <select id="count" parameterType="com.cms.sms.dao.domain.ProductByCustomer" resultType="_long">
		SELECT
			COUNT(*)
		FROM
			tbl_productByCustomer
		<include refid="whereStatements" />
    </select>

    <insert id="create" parameterType="com.cms.sms.dao.domain.ProductByCustomer">
		INSERT INTO
			tbl_productByCustomer
		(
		    customerNo,
		    productCode,
		    discountPrice
		)
		VALUES
		(
			#{customerNo},
			#{productCode},
			#{discountPrice}
		)
    </insert>

    <insert id="createAll" parameterType="list">
		INSERT INTO
			tbl_productByCustomer
		(
		    customerNo,
		    productCode,
		    discountPrice
		)
		VALUES
		<foreach collection="list" item="com.cms.sms.dao.domain.ProductByCustomer" separator=",">
		(
			#{customerNo},
			#{productCode},
			#{discountPrice}
		)
		</foreach>
	</insert>

	<update id="update" parameterType="com.cms.sms.dao.domain.ProductByCustomer">
		UPDATE
			tbl_productByCustomer
		<set>
			<if test="discountPrice != null">discountPrice = #{discountPrice}, </if>
		</set>
		WHERE
			customerNo =  #{customerNo}
			AND productCode =  #{productCode}
	</update>

	<delete id="delete" parameterType="com.cms.sms.dao.domain.ProductByCustomer">
		DELETE FROM
				tbl_productByCustomer
		WHERE
			customerNo =  #{customerNo}
			AND productCode =  #{productCode}
	</delete>

</mapper>
